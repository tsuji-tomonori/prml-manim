# PRML 5.6 混合密度ネットワーク ナレーション台本

対象: PRML Chapter 5, Section 5.6 Mixture Density Networks

## 構成

| Scene | 内容 | 画面で見せること | 原文参照 |
|---:|---|---|---|
| 1 | 逆問題と多峰性 | 順問題と逆問題、平均予測の破綻 | Fig. 5.18, Fig. 5.19 |
| 2 | 条件付き混合分布 | `p(t|x)` をガウス混合で表す | Eq. (5.148) |
| 3 | ネットワーク出力の制約変換 | softmax、exp、identity | Eq. (5.149)-(5.152), Fig. 5.20 |
| 4 | 尤度で学習する | 負の対数尤度と責務 `gamma_k` | Eq. (5.153)-(5.157) |
| 5 | 入力で変わる密度 | `pi_k(x)`, `mu_k(x)`, `sigma_k(x)` による密度地図 | Fig. 5.21(a)-(c) |
| 6 | 平均とモード | 条件付き平均と近似モードの違い | Eq. (5.158)-(5.160), Fig. 5.21(d) |
| 7 | まとめ | MDN の構成と使いどころ | Section 5.6 |

## scene01: 逆問題と多峰性

- 五章六節では、混合密度ネットワークを使って、ニューラルネットワークの出力を一つの値から条件付き分布へ広げます。
- 普通の回帰では、入力エックスが決まると、ターゲットの平均を一つ返すと考えがちです。
- しかし逆問題では、同じ観測に対して、正しい答えが二つ以上あることがあります。
- そのとき平均だけを返すと、どの正解にも近くない中間の値を選んでしまいます。
- 混合密度ネットワークは、一つの予測値ではなく、あり得る答えの分布を出すための方法です。

## scene02: 条件付き混合分布

- 目標は、入力エックスを条件にしたターゲットの分布、ピー、ティー、バー、エックスを表すことです。
- PRML では、ガウス分布を複数混ぜた形を使います。
- 各成分には、重みパイ、中心ミュー、幅シグマがあります。
- 重要なのは、この三つが固定値ではなく、入力エックスの関数になることです。
- エックスごとに混合比、中心、幅を変えれば、単峰にも多峰にもなる条件付き分布を表せます。

## scene03: ネットワーク出力から分布パラメータへ

- 混合密度ネットワークでは、普通のニューラルネットワークが混合分布のパラメータを出力します。
- 混合係数はゼロ以上で、全部足すと一になる必要があるので、ソフトマックスで変換します。
- シグマは正でなければならないので、指数関数で変換します。
- ミューは実数をそのまま取れるので、出力活性を直接使えます。
- ネットワークの生の出力を、確率分布として意味を持つ値へ変換するのがポイントです。

## scene04: 尤度で学習する

- 学習では、観測されたターゲットに高い密度を与えるように、重みを調整します。
- 二乗誤差ではなく、混合分布の負の対数尤度を最小化します。
- ある点がどの成分から出たらしそうかは、ガンマ、つまり事後的な責務として計算できます。
- この責務を使うと、混合係数、中心、幅に対する誤差信号を通常の逆伝播で流せます。
- MDN の学習は、分布全体の尤度を上げるように、成分ごとの責務を使って進みます。

## scene05: 入力で変わる密度

- 学習後のモデルを、条件付き密度の地図として見てみます。
- 横軸が入力エックス、縦軸がターゲットティーで、明るい場所ほど確率密度が高い場所です。
- 左端や右端では一つの山だけになり、中央では三つの山が同時に現れます。
- ネットワーク出力は連続な関数ですが、混合比を変えることで、密度の形は多峰になります。
- MDN は、各エックスで必要なだけの候補を、密度として残せます。

## scene06: 平均とモード

- 条件付き分布が手に入ると、そこから必要な要約量を選べます。
- 条件付き平均は、成分の中心を混合係数で重み付き平均したものです。
- ただし多峰分布では、平均が低密度の谷に落ちることがあります。
- 実際の制御や選択では、一番重みの大きい成分の中心、つまり近似的なモードを使うほうが自然な場合があります。
- MDN は平均だけでなく、モードや分散など、用途に合った予測を後から選べます。

## scene07: まとめ

- 混合密度ネットワークは、ニューラルネットワークと混合モデルを組み合わせた条件付き密度モデルです。
- ネットワークは、入力からパイ、ミュー、シグマを出力し、ソフトマックスと指数関数で制約を満たします。
- 負の対数尤度を最小化すれば、通常の逆伝播で学習できます。
- 逆問題のように答えが複数ある場面では、平均の一点予測よりも、分布を持つ予測のほうが情報を失いません。
- 五章六節の核心は、回帰を一つの点から、条件付き確率密度の予測へ拡張することです。
